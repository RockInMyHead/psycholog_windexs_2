# Исправление HTTP 429 ошибок (Rate Limiting)

## 🔍 Проблема

При использовании голосового чата (особенно TTS - синтез речи) возникают HTTP 429 ошибки:

```
HTTP 429: Too Many Requests
Apache/2.4.65 (Debian) Server at psycholog.windexs.ru Port 443
```

**Причина**: Rate limiting настроен на уровне веб-сервера (Apache или Nginx) и блокирует частые запросы к API.

## 🛠️ Решение

### Шаг 1: Определить используемый веб-сервер

Проверьте, какой веб-сервер используется:

```bash
# На сервере выполните:
sudo systemctl status nginx
sudo systemctl status apache2

# Проверьте какой процесс слушает порт 443:
sudo netstat -tlnp | grep :443
# или
sudo ss -tlnp | grep :443
```

### Шаг 2: Применить конфигурацию Nginx

#### 2.1 Если используется Nginx:

```bash
# Скопируйте обновленную конфигурацию
sudo cp nginx.conf /etc/nginx/sites-available/psycholog.windexs.ru

# Проверьте синтаксис
sudo nginx -t

# Перезагрузите Nginx
sudo systemctl reload nginx
```

#### 2.2 Если используется Apache:

Apache требует другой конфигурации. Создайте файл `/etc/apache2/conf-available/rate-limit.conf`:

```apache
# Disable rate limiting for API endpoints
<Location "/api/audio/speech">
    # Allow more requests for TTS
    # If using mod_evasive:
    DOSPageCount 50
    DOSPageInterval 1
    DOSSiteCount 100
    DOSSiteInterval 1
    DOSBlockingPeriod 10
</Location>

<Location "/api/audio/transcriptions">
    # Allow more requests for transcription
    DOSPageCount 30
    DOSPageInterval 1
    DOSSiteCount 100
    DOSSiteInterval 1
    DOSBlockingPeriod 10
</Location>

<Location "/api">
    # General API rate limiting
    DOSPageCount 100
    DOSPageInterval 1
    DOSSiteCount 200
    DOSSiteInterval 1
    DOSBlockingPeriod 10
</Location>
```

Затем:

```bash
# Включите конфигурацию
sudo a2enconf rate-limit

# Перезагрузите Apache
sudo systemctl reload apache2
```

### Шаг 3: Если Apache стоит перед Nginx

Если у вас Apache перед Nginx (reverse proxy), нужно настроить оба:

1. **Apache** - увеличить лимиты как показано выше
2. **Nginx** - применить обновленную конфигурацию из `nginx.conf`

### Шаг 4: Альтернативное решение - отключить rate limiting

Если проблема критична, можно временно отключить rate limiting:

#### Для Apache:

```bash
# Отключить mod_evasive
sudo a2dismod evasive
sudo systemctl reload apache2
```

#### Для Nginx:

Удалите или закомментируйте строки `limit_req` в конфигурации:

```nginx
# Закомментируйте эти строки:
# limit_req zone=api_tts burst=10 nodelay;
# limit_req_status 429;
```

## 📋 Обновленная конфигурация Nginx

Файл `nginx.conf` теперь включает:

1. **Rate limiting зоны**:
   - `api_general` - 30 req/s для обычных API запросов
   - `api_audio` - 10 req/s для транскрибации
   - `api_tts` - 5 req/s для синтеза речи

2. **Burst параметры** для обработки всплесков трафика:
   - General API: burst=20
   - Audio transcription: burst=15
   - TTS: burst=10

3. **Отдельные location блоки** для каждого типа запросов с оптимизированными настройками

## 🧪 Тестирование

После применения изменений:

1. Перезагрузите веб-сервер
2. Очистите кэш браузера
3. Попробуйте использовать голосовой чат
4. Проверьте логи:

```bash
# Nginx логи:
sudo tail -f /var/log/nginx/psycholog_error.log
sudo tail -f /var/log/nginx/psycholog_access.log

# Apache логи (если используется):
sudo tail -f /var/log/apache2/error.log
sudo tail -f /var/log/apache2/access.log
```

## 🔍 Диагностика

Если проблема сохраняется:

1. **Проверьте, какой сервер отвечает**:
   ```bash
   curl -I https://psycholog.windexs.ru/api/audio/speech
   ```

2. **Проверьте активные rate limiting правила**:
   ```bash
   # Nginx
   sudo nginx -T | grep limit_req
   
   # Apache
   apachectl -M | grep evasive
   ```

3. **Проверьте логи на наличие 429 ошибок**:
   ```bash
   sudo grep "429" /var/log/nginx/*.log
   sudo grep "429" /var/log/apache2/*.log
   ```

## 🚀 Рекомендации

1. **Мониторинг**: Настройте мониторинг rate limiting для предотвращения злоупотреблений
2. **Аутентификация**: Используйте токены для разных лимитов на пользователя
3. **CDN**: Рассмотрите использование CDN для статических ресурсов
4. **Кэширование**: Кэшируйте TTS ответы для повторяющихся фраз

## ⚠️ Важно

- Обновленная конфигурация находится в репозитории: `nginx.conf`
- Не забудьте сделать backup текущей конфигурации перед изменениями
- После изменений всегда проверяйте синтаксис: `sudo nginx -t` или `sudo apachectl configtest`
