EMERGENCY FIX COMMAND
=====================

ERROR: "Cannot access uninitialized variable" persists
REASON: Old JavaScript bundle still active

CURRENT STATUS:
- Browser loads: index-Dtn52uhQ.js (OLD - BROKEN)
- Server should have: index-D9_ZgnPJ.js (NEW - FIXED)

CRITICAL FIX:
=============

Run these commands ON THE SERVER:

1. Check current files:
   ls -la /var/www/psycholog.windexs.ru/assets/

2. If old file exists, backup and remove:
   cd /var/www/psycholog.windexs.ru
   mkdir -p backup
   cp assets/index-Dtn52uhQ.js backup/ 2>/dev/null || true
   rm -f assets/index-Dtn52uhQ.js

3. Extract new files:
   tar -xzf ~/audio-fix-deploy.tar.gz

4. Verify:
   ls -la assets/index-D9_ZgnPJ.js

5. Reload nginx:
   systemctl reload nginx

6. Test:
   curl -s "https://psycholog.windexs.ru" | grep "index-.*\.js"

EXPECTED RESULT:
- index-D9_ZgnPJ.js in HTML
- No JavaScript errors
- Audio calls work

IF STILL BROKEN:
- Clear browser cache
- Check file permissions
- Verify nginx config points to correct directory