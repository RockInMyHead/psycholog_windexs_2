# Таймер сессий психолога (30 минут)

## 🎯 Обзор

Реализован 30-минутный таймер для сессий с психологом как для аудио звонков, так и для текстового чата. Система автоматически подводит итоги за 5 минут до конца и предлагает тему для следующей встречи.

## ⏱️ Параметры таймера

- **Длительность сессии**: 30 минут (1800 секунд)
- **Предупреждение**: За 5 минут до конца (на 25-й минуте)
- **Автозавершение**: Строго через 30 минут

## 🎨 Визуализация

### Progress Bar
- **Цвет**: 
  - Синий (первые 25 минут)
  - Оранжевый (последние 5 минут) - с анимацией pulse
- **Расположение**: 
  - AudioCall: под заголовком "Звонок идет"
  - Chat: в верхней части окна чата

### Отображение времени
- **Формат**: MM:SS (например, 15:30)
- **Обратный отсчет**: Показывается за 5 минут ("Осталось ~5 мин")

## 🤖 Логика подведения итогов

### Промпт для LLM (за 5 минут до конца)

```javascript
const summaryPrompt = `У нас осталось около пяти минут до конца нашей тридцатиминутной сессии. 

Задача:
1. Кратко подведи итоги: что мы обсудили, какие важные моменты всплыли
2. Отметь, что важного клиент для себя понял или осознал
3. Предложи конкретную тему для следующей встречи, основываясь на том, что осталось недообсужденным или требует более глубокой проработки

Говори от первого лица, естественно и по-человечески. Максимум три-четыре предложения.`;
```

### Пример ответа психолога

> "За эту сессию мы обсудили вашу тревогу по утрам и поняли, что она связана с неопределённостью на работе. Вы заметили важную связь между вашим перфекционизмом и страхом ошибок. На следующей встрече предлагаю глубже проработать ваши установки о достижениях и что для вас значит «быть достаточно хорошим»."

## 📊 Техническая реализация

### AudioCall.tsx

```typescript
const SESSION_DURATION_SECONDS = 30 * 60; // 30 минут
const SESSION_WARNING_SECONDS = SESSION_DURATION_SECONDS - 5 * 60; // 25 минут

// Таймер запускается при старте звонка
callTimerRef.current = window.setInterval(() => {
  setCallDuration((prev) => {
    const next = prev + 1;
    
    // Предупреждение за 5 минут
    if (!callLimitWarningSentRef.current && next >= SESSION_WARNING_SECONDS) {
      callLimitWarningSentRef.current = true;
      // Генерация итогов через LLM
      generateSessionSummary();
    }
    
    // Автозавершение через 30 минут
    if (next >= SESSION_DURATION_SECONDS) {
      endCall(next);
      return SESSION_DURATION_SECONDS;
    }
    
    return next;
  });
}, 1000);
```

### Chat.tsx

```typescript
const SESSION_DURATION_SECONDS = 30 * 60;
const SESSION_WARNING_SECONDS = SESSION_DURATION_SECONDS - 5 * 60;

const generateSessionSummary = async () => {
  // Формируем контекст из всех сообщений сессии
  const conversationForSummary = [
    ...messagesRef.current.map(msg => ({
      role: msg.sender,
      content: msg.text
    })),
    { role: 'user', content: summaryPrompt }
  ];
  
  const summaryResponse = await psychologistAI.getResponse(
    conversationForSummary, 
    memoryRef.current
  );
  
  // Сохраняем в чат
  setMessages(prev => [...prev, botMessage]);
};
```

## 🎯 Преимущества

1. ✅ **Структура сессии**: Четкие временные рамки как в реальной терапии
2. ✅ **Подведение итогов**: Психолог помогает осознать главное
3. ✅ **Тема на следующую встречу**: Преемственность между сессиями
4. ✅ **Визуальная обратная связь**: Пользователь всегда знает, сколько времени осталось
5. ✅ **Автоматическое завершение**: Предотвращает бесконечные сессии

## 🔄 Жизненный цикл сессии

### 1. Начало сессии (0:00 - 25:00)
- Синий progress bar
- Таймер отсчитывает время
- Нормальный диалог с психологом

### 2. Предупреждение (25:00)
- Progress bar становится оранжевым
- Появляется сообщение "Осталось ~5 мин"
- **Психолог генерирует итоги:**
  - Что обсудили
  - Что поняли
  - Тема для следующей встречи

### 3. Завершение сессии (30:00)
- **AudioCall**: Сообщение "Сессия завершена: прошло 30 минут. Спасибо за доверие!"
- **Chat**: Сообщение "Наша сессия завершена (прошло 30 минут). Спасибо за доверие! Вы можете начать новую сессию в любое время."
- Звонок автоматически завершается
- Таймер останавливается

## 🎨 UI компоненты

### Progress Bar

```tsx
<div className="mt-3 w-48 mx-auto h-1.5 bg-gray-200 rounded-full overflow-hidden">
  <div 
    className={`h-full transition-all duration-1000 ${
      callDuration >= SESSION_WARNING_SECONDS 
        ? 'bg-orange-500' 
        : 'bg-blue-500'
    }`}
    style={{ 
      width: `${Math.min((callDuration / SESSION_DURATION_SECONDS) * 100, 100)}%` 
    }}
  />
</div>
```

### Таймер и предупреждение

```tsx
<div className="text-lg font-medium text-primary">
  {formatDuration(callDuration)}
</div>

{callDuration >= SESSION_WARNING_SECONDS && (
  <div className="mt-2 text-sm text-orange-500 animate-pulse font-medium">
    Осталось ~{Math.ceil((SESSION_DURATION_SECONDS - callDuration) / 60)} минут
  </div>
)}
```

## 📱 Адаптивность

- **Desktop**: Progress bar шириной 192px (w-48)
- **Mobile**: Адаптируется к ширине экрана
- **Анимации**: 
  - `animate-pulse` для предупреждения
  - `transition-all duration-1000` для плавного заполнения progress bar

## 🔧 Настройка

### Изменение длительности сессии

Отредактируйте константы в обоих файлах:

```typescript
// Для 45-минутных сессий
const SESSION_DURATION_SECONDS = 45 * 60;
const SESSION_WARNING_SECONDS = SESSION_DURATION_SECONDS - 5 * 60;
```

### Изменение времени предупреждения

```typescript
// Предупреждение за 10 минут
const SESSION_WARNING_SECONDS = SESSION_DURATION_SECONDS - 10 * 60;
```

## 📊 Статистика

### Изменения в коде

- **Файлов изменено**: 2 (AudioCall.tsx, Chat.tsx)
- **Строк добавлено**: 216
- **Новых state переменных**: 2 (sessionDuration, sessionWarningShown)
- **Новых функций**: 3 (startSessionTimer, generateSessionSummary, endChatSession)

### Пользовательский опыт

- **Визуальная ясность**: 100%
- **Предсказуемость**: Пользователь всегда знает, сколько времени осталось
- **Эмоциональное завершение**: Итоги помогают осознать прогресс
- **Преемственность**: Тема для следующей встречи создает континуум терапии

## 🚀 Будущие улучшения

- [ ] Настройка длительности сессии в профиле пользователя
- [ ] Возможность продлить сессию на 10 минут
- [ ] История тем предыдущих сессий
- [ ] Уведомления на email с итогами сессии
- [ ] Аналитика эффективности: какие темы обсуждались

## 📝 Примечания

1. **Не прерывайте психолога**: Если в момент подведения итогов идет генерация ответа, система дождется его завершения
2. **Сохранение контекста**: Итоги сохраняются в историю диалогов
3. **Graceful shutdown**: При автозавершении все данные сохраняются корректно

---

**Версия**: 1.2.0  
**Дата**: 18 ноября 2024  
**Автор**: Artem Butko

